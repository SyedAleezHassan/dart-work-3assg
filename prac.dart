void main() {
  // bool x;
  // num A = 2, B = 5;
  // x = (A == 2 && B == 5) && (A > 2 || B == 5);
  // print(x);
  // List Names = [11, 12, 14, 15, 16, 56];
  // Names.add(2);
  // print(Names);
  // String name = "syed aleez hassan";
  // num lengthOfName = name.length;
  // print(lengthOfName);
  }
